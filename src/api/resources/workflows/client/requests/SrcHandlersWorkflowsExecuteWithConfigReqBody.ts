// This file was auto-generated by Fern from our API Definition.

import * as Scout from "../../../../index.js";

/**
 * @example
 *     {
 *         environment: "environment",
 *         revision_id: "revision_id",
 *         session_id: "session_id",
 *         workflow_config: {}
 *     }
 */
export interface SrcHandlersWorkflowsExecuteWithConfigReqBody {
    /**
     * Specifies the execution environment for the workflow. The available environments include:
     * - `production`: The production environment, where workflows are executed under live conditions.
     * - `staging`: A staging environment used for testing prior to production deployment.
     * - `development`: A development environment used for testing new changes.
     * - `console`: The console environment, runs latest changes on a workflow.
     */
    environment?: string;
    revision_id?: string;
    session_id?: string;
    workflow_key?: string;
    inputs?: Record<string, SrcHandlersWorkflowsExecuteWithConfigReqBody.Inputs.Value>;
    streaming?: boolean;
    workflow_config: Scout.WorkflowConfigInput;
}

export namespace SrcHandlersWorkflowsExecuteWithConfigReqBody {
    export namespace Inputs {
        export type Value = boolean | Record<string, unknown> | number | number | unknown[] | string;
    }
}
