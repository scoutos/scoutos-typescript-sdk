// This file was auto-generated by Fern from our API Definition.

/**
 * Configuration for what content to include in briefing.
 */
export interface BriefingContentConfig {
    metrics?: BriefingContentConfig.Metrics.Item[];
    sections?: BriefingContentConfig.Sections.Item[];
    composable_sections?: Record<string, unknown>[];
}

export namespace BriefingContentConfig {
    export type Metrics = Metrics.Item[];

    export namespace Metrics {
        export const Item = {
            Closed: "closed",
            Target: "target",
            Gap: "gap",
            AtRisk: "at_risk",
            Pipeline: "pipeline",
            Created: "created",
        } as const;
        export type Item = (typeof Item)[keyof typeof Item];
    }

    export type Sections = Sections.Item[];

    export namespace Sections {
        export const Item = {
            WhatChanged: "what_changed",
            FocusDeals: "focus_deals",
            PipelineHealth: "pipeline_health",
            Recommendations: "recommendations",
            RepBreakdown: "rep_breakdown",
            StageAnalysis: "stage_analysis",
        } as const;
        export type Item = (typeof Item)[keyof typeof Item];
    }
}
