// This file was auto-generated by Fern from our API Definition.

export interface CreateDriveNodeRequest {
    /** Type of resource (tables, workflow, crawl, file, integration, directory) */
    resource_type: CreateDriveNodeRequest.ResourceType;
    /** Full path for the node (e.g., '/Projects/MyWorkflow') */
    resource_path: string;
    /** ID of the underlying resource (ignored for directories) */
    resource_id: string;
}

export namespace CreateDriveNodeRequest {
    /** Type of resource (tables, workflow, crawl, file, integration, directory) */
    export const ResourceType = {
        Tables: "tables",
        Workflow: "workflow",
        Crawl: "crawl",
        File: "file",
        Integration: "integration",
        Directory: "directory",
    } as const;
    export type ResourceType = (typeof ResourceType)[keyof typeof ResourceType];
}
