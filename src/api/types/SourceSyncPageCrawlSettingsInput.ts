/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Scout from "../index.js";

export interface SourceSyncPageCrawlSettingsInput {
    source_archetype_id: "com.scoutos.page_crawl";
    /** The starting URL for the crawl process. */
    start_urls: string;
    crawler_settings?: Scout.SinglePageCrawlerSettingsInput;
    /** Merge: upsert documents keying off site url. Replace: merge and remove stale documents. */
    mode?: SourceSyncPageCrawlSettingsInput.Mode;
}

export namespace SourceSyncPageCrawlSettingsInput {
    /**
     * Merge: upsert documents keying off site url. Replace: merge and remove stale documents.
     */
    export type Mode = "merge" | "replace";
    export const Mode = {
        Merge: "merge",
        Replace: "replace",
    } as const;
}
