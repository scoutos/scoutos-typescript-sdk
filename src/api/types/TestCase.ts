// This file was auto-generated by Fern from our API Definition.

import type * as Scout from "../index.js";

/**
 * A single test case for workflow evaluation
 */
export interface TestCase {
    /** Test case name/description */
    name: string;
    /** Input data to pass to workflow (matches workflow input schema) */
    input: Record<string, unknown>;
    /** Expected output from workflow */
    expected_output?: Record<string, unknown>;
    /** [LEGACY] Type-safe assertions to check (use scorers instead) */
    assertions?: TestCase.Assertions.Item[];
    /** Scorer configurations for this test case */
    scorers?: Scout.ScorerConfigRuntime[];
    /** Additional metadata (e.g., row_id, tags) */
    metadata?: Record<string, unknown>;
}

export namespace TestCase {
    export type Assertions = Assertions.Item[];

    export namespace Assertions {
        export type Item =
            | Scout.ContainsAssertion
            | Scout.ExactMatchAssertion
            | Scout.FieldExistsAssertion
            | Scout.LlmJudgeAssertion
            | Scout.RegexMatchAssertion
            | Scout.SemanticSimilarityAssertion
            | Scout.TypeCheckAssertion;
    }
}
