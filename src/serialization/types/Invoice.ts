/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as serializers from "../index";
import * as Scout from "../../api/index";
import * as core from "../../core";
import { InvoiceMeta } from "./InvoiceMeta";
import { InvoiceStatus } from "./InvoiceStatus";
import { LineItem } from "./LineItem";
import { StripeProviderMeta } from "./StripeProviderMeta";

export const Invoice: core.serialization.ObjectSchema<serializers.Invoice.Raw, Scout.Invoice> =
    core.serialization.object({
        id: core.serialization.string(),
        meta: InvoiceMeta,
        status: InvoiceStatus,
        description: core.serialization.string(),
        line_items: core.serialization.list(LineItem),
        created_at: core.serialization.date(),
        paid_at: core.serialization.date().optional(),
        updated_at: core.serialization.date(),
        provider_meta: StripeProviderMeta.optional(),
    });

export declare namespace Invoice {
    export interface Raw {
        id: string;
        meta: InvoiceMeta.Raw;
        status: InvoiceStatus.Raw;
        description: string;
        line_items: LineItem.Raw[];
        created_at: string;
        paid_at?: string | null;
        updated_at: string;
        provider_meta?: StripeProviderMeta.Raw | null;
    }
}
