/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as serializers from "../index";
import * as Scout from "../../api/index";
import * as core from "../../core";
import { Tool } from "./Tool";

export const ToolsData: core.serialization.ObjectSchema<serializers.ToolsData.Raw, Scout.ToolsData> =
    core.serialization.object({
        available: core.serialization.list(Tool),
        unavailable: core.serialization.list(Tool),
    });

export declare namespace ToolsData {
    export interface Raw {
        available: Tool.Raw[];
        unavailable: Tool.Raw[];
    }
}
